I’ll verify the approved package’s manifest, review PASS, trial PASS, and deployment target first. Shipping requires all of those gates plus operator approval; if they are present, I’ll deploy the package and confirm the governance artifacts are deployed and verified by read-back. The prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers.