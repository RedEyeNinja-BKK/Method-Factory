I’ll verify the package manifest, review PASS, trial PASS, and explicit operator approval before shipping. I’ll also confirm that **the prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers**. If any required gate is missing or failed, I’ll stop and report exactly what is needed; otherwise I’ll ship and verify the governance artifacts by read-back.