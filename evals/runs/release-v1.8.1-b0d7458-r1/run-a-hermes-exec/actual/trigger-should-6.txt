I can ship only after verifying the package’s durable manifest, review PASS, trial PASS, and operator approval. I’m checking those records now rather than assuming which package “approved” refers to. The governance artifacts, if present, will be deployed as required: **the prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers.**
