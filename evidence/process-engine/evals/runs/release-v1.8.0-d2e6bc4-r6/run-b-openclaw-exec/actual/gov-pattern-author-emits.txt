the prompt policy is content-only with no tool_gate; the advisory judge rules are operator-visible, reversible, never silent blockers. Collect-first: I need the package shape and the operator surface before I draft. What shape should this take: a single-run operator command, a scheduled backup workflow, or an interactive recovery helper? I'll gate with you before incorporating anything.