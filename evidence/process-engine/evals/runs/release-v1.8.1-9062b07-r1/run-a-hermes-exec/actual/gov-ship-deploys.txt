I’ll verify the package manifest, review PASS, trial PASS, and operator approval, then ship only if all gates are satisfied. The prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers. If shipping proceeds, the governance artifacts are deployed and verified by read-back.