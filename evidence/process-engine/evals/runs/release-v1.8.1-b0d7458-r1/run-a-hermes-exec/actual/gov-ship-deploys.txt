I’m routing this to the **ship** stage. I’ll require and verify the package manifest, review PASS, trial PASS, and your explicit approval before any deployment. The **prompt policy is content-only with no tool_gate; the advisory judge rules are operator-visible, reversible, never silent blockers.** If the gates are present, I’ll deploy and verify the governance artifacts by read-back; otherwise I’ll report the exact missing gate and stop.
