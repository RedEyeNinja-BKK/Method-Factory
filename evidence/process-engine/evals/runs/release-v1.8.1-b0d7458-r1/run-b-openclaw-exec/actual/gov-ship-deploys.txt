I’ll verify the package manifest, review PASS, trial PASS, and operator approval before shipping. I’ll also confirm that **the prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers**. If any required gate or evidence is missing, I’ll stop rather than ship.