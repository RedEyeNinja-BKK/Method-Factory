I’ll verify the package manifest, review **PASS**, trial **PASS**, and operator approval before shipping. If all gates are present, I’ll deploy the governance artifacts—the prompt policy is content-only with no `tool_gate`; the advisory judge rules are operator-visible, reversible, never silent blockers—and verify the governance artifacts by read-back.