We're generating the package for: {intent}.

Since authoring is authorized (the summary was confirmed by the operator), I'll generate the package content and propose the draft through a `record_draft_artifact` Action Envelope for the code layer to validate.

Before I draft it: do you have any final inputs to add? Share anything useful, or say that's all.
