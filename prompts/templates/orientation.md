Method Factory helps turn an idea, intent, and any useful material into a prompt-and-code package. The code layer keeps the package record; this conversation helps shape what the package should contain.

What do you want to build?

If you have anything to work from, share it: a link, pasted text, a file, an example, notes, constraints, or anything else that may help. Nothing is rejected by type.
