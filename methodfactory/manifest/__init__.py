"""Manifest package — canonical serialization, schema, and renders.

The JSONL-era store module was removed in the persistence reset (ADR-0012 §8);
this package holds the storage-independent manifest primitives.
"""
